# Dependências do crawler_contador_questoes.py
# Instale com:  pip install -r requirements-crawler.txt

# Obrigatórias
requests>=2.28.0
beautifulsoup4>=4.11.0

# Anti-Cloudflare (recomendado — instale ao menos uma das duas)
cloudscraper>=1.2.60       # mais simples; suficiente quando cf_clearance é fornecido
# curl_cffi>=0.6.0         # fingerprint TLS nativo; mais robusto; compilação necessária

# Leitura do arquivo .env
python-dotenv>=1.0.0
